services:
  - type: web
    name: himchistka-crm
    env: static
    staticPublishPath: .
    buildCommand: echo "No build needed"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
